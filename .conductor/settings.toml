"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "corepack enable && yarn install"
run_mode = "concurrent"

[scripts.run.dev]
available_in = [ "local" ]
command = "yarn dev --port $CONDUCTOR_PORT --force"
default = true
icon = "play"
